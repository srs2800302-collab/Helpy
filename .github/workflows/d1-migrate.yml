name: D1 Migrate

on:
  workflow_dispatch:

jobs:
  inspect-migrations:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Inspect D1 migration plan
        run: apps/api/scripts/d1-migration-plan.sh
